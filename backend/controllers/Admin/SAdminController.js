const SAdmindb = require ("../../model/getDbData")

exports.createAdmin = async(req,res)=>{
    try {
        const {username, password, name,}
        const data = await 
    } catch (error) {
        
    }
}